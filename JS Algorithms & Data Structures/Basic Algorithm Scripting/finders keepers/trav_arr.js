// traverse an array
var arr = [1,2,3,4,5];

// traverses array and outputs each element individually 
for (let i = 0; i < arr.length; i++) {
    // return arr;
    console.log(arr[i]);
};

// console.log(arr[0]); // outputs array element at specified index

