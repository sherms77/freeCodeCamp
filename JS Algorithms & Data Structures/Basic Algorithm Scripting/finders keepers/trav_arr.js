// traverse an array
var arr = [1,2,5];

// traverses array and outputs each element individually 
for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
};

console.log(arr[2]); // outputs array element at specified index

