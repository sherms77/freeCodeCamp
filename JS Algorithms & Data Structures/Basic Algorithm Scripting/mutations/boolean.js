const string1 = "zyxwvutsrqponmlkjihgfedcba";
const string2 = "qrstu";

// does not work. strings are not the same so will retrun false.
// not character based comparison
if (string1 == string2) {
    console.log("the strings have the same characters")
} 
else(console.log("does not contain string"));