function getNine() {
  let x = 6;
  let y = 3;
  return x + y;
}

let result = getNine(); // put parenthesis at the end of the 'getNine()' function to call the result of the function and store it in the variable 'result'.
console.log(result);
