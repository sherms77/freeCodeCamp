let receivables = 10;
let payables = 8;
let netWorkingCapital = receivables - payables; // two errors -> 'receivables' and 'payables' were mispelled
console.log(`Net working capital is: ${netWorkingCapital}`);